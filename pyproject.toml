[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "prism"
version = "0.9.0"
dependencies = [
    "numpy",
    "scipy",
    "pyscf"
]

[tool.setuptools]
packages = ["prism"]
package-dir = {"prism" = "."}
